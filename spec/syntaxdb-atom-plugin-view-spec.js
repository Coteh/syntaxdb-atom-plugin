'use babel';

import SyntaxdbAtomPluginView from '../lib/syntaxdb-atom-plugin-view';

describe('SyntaxdbAtomPluginView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
